# TODO: Crawler 내재화

## 배경
- 현재 크롤링 기능은 외부 서비스로 분리되어 운영 중임.
- 대상 저장소: https://github.com/Aha00a/crawler
- 해당 프로젝트는 오픈소스이며, 작성자가 동일하여 라이선스 이슈는 없음.
- 외부 링크 대상 크롤링은 이미 동작 중이며, 이를 AhaWiki 내부로 통합하고자 함.

## 목표
- 외부 crawler 서비스 의존성을 제거하고 AhaWiki 내부 API로 내재화.
- 비동기 처리 기반으로 안정적인 크롤링 수행.
- 크롤링 결과를 DB 테이블에 캐싱하여 성능 및 재활용성 확보.

## 비목표 (Non-goals)
- [ ] 최초 릴리즈에서 자바스크립트 렌더링(헤드리스 브라우저)까지 지원하지 않음.
- [ ] 사이트별 파서 고도화보다 "안전한 기본 크롤링 + 일관된 실패 처리"를 우선함.

## 범위
- crawler 저장소 코드/로직 분석 및 필요한 컴포넌트 선별 또는 대체 라이브러리 도입.
- AhaWiki 코드베이스 내 크롤링 내부 API 도입.
- 기존 crawler 호출 경로를 내부 API 호출로 대체.
- 비동기 작업 처리(큐/워커) 방식 적용.
- 크롤링 결과 캐싱용 테이블 설계 및 적용.
- 캐시 TTL 6개월 적용.
- 캐시 관리 기능(조회/삭제/강제갱신 등) 제공.
- 로깅 및 에러처리 표준화.

## 요구사항 명확화 (문제 가능 지점 보완)
- [ ] URL 입력은 **https만 허용** (`http`는 거부).
- [ ] URL 정규화 규칙 정의 (host lowercase, trailing slash 정리, fragment 제거 등).
- [ ] 캐시 키를 원본 URL이 아닌 **정규화 URL + 해시** 기준으로 통일.
  - [ ] 기대효과: 중복 캐시 감소, 동일 문서 캐시 hit rate 상승, URL 표현 차이로 인한 재크롤링 낭비 방지.
- [ ] 만료 정책을 절대시간(`expires_at`) + 갱신정책(SWR 여부)으로 분리 정의.
- [ ] "만료/미존재 시 비동기 재수집" 시점의 API 응답 계약(202/200, stale 허용 여부) 명시.
- [ ] 타임아웃(연결/응답), 최대 본문 크기, 최대 리다이렉트 횟수 상한 명시.
- [ ] 중복 작업 방지(idempotency key, 동일 URL 동시 요청 dedup) 설계.
- [ ] 실패 재시도 정책을 상태코드/에러 유형별로 분리(예: 429/5xx 재시도, 4xx 일부 즉시 실패).

## 현재 상태 점검 체크리스트
- [ ] AhaWiki에서 외부 crawler를 호출하는 모든 지점 식별.
- [ ] 요청/응답 스키마, 에러코드, 재시도 정책 문서화.
- [ ] crawler 저장소의 핵심 기능/의존성/실행환경 파악.
- [ ] 현재 외부 링크 크롤링 동작 기준(입력/출력/실패 케이스) 고정.

## 내재화 설계 TODO
- [ ] 통합 방식 확정: **내부 API 방식**.
- [ ] 패키지 구조 설계 (예: `app/controllers/ApiCrawler.scala`, `app/logics/crawler/*`).
- [ ] 내부 API 인터페이스 설계
  - [ ] 입력 URL/옵션 모델
  - [ ] 결과 모델(본문/메타/에러)
  - [ ] 요청 접수/작업 상태 조회/결과 조회 API
  - [ ] API 버전(`v1`)과 하위호환 정책 정의
- [ ] 비동기 처리 설계
  - [ ] 작업 큐 모델
  - [ ] 워커 실행/재시도/백오프
  - [ ] 작업 상태(`queued`, `running`, `success`, `failed`) 관리
  - [ ] 동일 URL 동시 처리 락/중복제거
- [ ] DB 스키마 설계
  - [ ] 크롤링 결과 캐시 테이블
  - [ ] `dateInserted`, `dateUpdated`, `dateDone`, `expires_at` 컬럼
  - [ ] 인덱스(URL 해시, 만료시각, 상태)
- [ ] 보안 설계
  - [ ] SSRF 방어(사설 IP, loopback, link-local, metadata endpoint 차단)
  - [ ] 허용/차단 도메인 정책
  - [ ] 응답 크기 제한
  - [ ] 콘텐츠 타입 화이트리스트(`text/html`, `text/plain` 등)

## 구현 TODO
- [ ] 내부 crawler API 엔드포인트 구현.
- [ ] 비동기 작업 큐/워커 구현.
- [ ] 크롤링 결과 캐시 테이블 생성(evolution 포함).
- [ ] 캐시 TTL 6개월 정책 적용.
- [ ] 캐시 히트 시 즉시 응답, 만료/미존재 시 정책에 따라 `stale-while-revalidate` 또는 `202 Accepted` 응답.
- [ ] 기존 외부 API 호출부를 내부 API 호출로 교체.
- [ ] 관리자 캐시 관리 기능 구현
  - [ ] URL별 캐시 조회
  - [ ] 캐시 삭제
  - [ ] 강제 재크롤링 트리거
- [ ] 실패/예외 시 에러 메시지 및 재처리 규칙 정리.
- [ ] 운영 메트릭 추가(성공률, 평균 지연, 큐 적체, 도메인별 실패율).

## 테스트 TODO
- [ ] URL 스킴 제한(https only) 테스트.
- [ ] URL 정규화/해시 일관성 테스트.
- [ ] SSRF/차단 도메인/사설 IP 차단 테스트.
- [ ] 리다이렉트/타임아웃/대용량 본문 제한 테스트.
- [ ] 큐 중복제거/재시도/백오프 테스트.
- [ ] 캐시 TTL 만료/갱신(SWR 또는 202 흐름) 테스트.

## 완료 기준 (Definition of Done)
- [ ] AhaWiki에서 외부 crawler 서비스 호출이 제거됨.
- [ ] 내부 API + 비동기 처리로 크롤링이 안정적으로 동작함.
- [ ] 크롤링 결과 캐시 테이블 및 6개월 TTL 정책이 적용됨.
- [ ] 관리자 캐시 관리 기능(조회/삭제/강제갱신)이 동작함.
- [ ] 보안 요구사항(SSRF/크기제한/도메인 정책) 검증 완료.
- [ ] 테스트(단위/통합/비동기/캐시) 통과.
