# TODO: Crawler 내재화

## 배경
- 현재 크롤링 기능은 외부 서비스로 분리되어 운영 중임.
- 대상 저장소: https://github.com/Aha00a/crawler
- 해당 프로젝트는 오픈소스이며, 작성자가 동일하여 라이선스 이슈는 없음.
- 외부 링크 대상 크롤링은 이미 동작 중이며, 이를 AhaWiki 내부로 통합하고자 함.

## 목표
- 외부 crawler 서비스 의존성을 제거하고 AhaWiki 내부 API로 내재화.
- 비동기 처리 기반으로 안정적인 크롤링 수행.
- 크롤링 결과를 DB 테이블에 캐싱하여 성능 및 재활용성 확보.

## 범위
- crawler 저장소 코드/로직 분석 및 필요한 컴포넌트 선별. 또는 다른 라이브러리 도입
- AhaWiki 코드베이스 내 크롤링 내부 API 도입.
- 기존 crawler 호출 경로를 내부 API 호출로 대체.
- 비동기 작업 처리(큐/워커) 방식 적용.
- 크롤링 결과 캐싱용 테이블 설계 및 적용.
- 캐시 만료(Expiration) 정책 6개월 적용.
- 캐시 관리 기능(조회/삭제/강제갱신 등) 제공.
- 로깅 및 에러처리 표준화.

## 현재 상태 점검 체크리스트
- [ ] AhaWiki에서 외부 crawler를 호출하는 모든 지점 식별.
- [ ] 요청/응답 스키마, 에러코드, 재시도 정책 문서화.
- [ ] crawler 저장소의 핵심 기능/의존성/실행환경 파악.
- [ ] 현재 외부 링크 크롤링 동작 기준(입력/출력/실패 케이스) 고정.

## 내재화 설계 TODO
- [ ] 통합 방식 확정: **내부 API 방식**.
- [ ] 패키지 구조 설계 (예: `app/controllers/ApiCrawler.scala`, `app/logics/crawler/*`).
- [ ] 내부 API 인터페이스 설계
  - [ ] 입력 URL/옵션 모델
  - [ ] 결과 모델(본문/메타/에러)
  - [ ] 요청 접수/작업 상태 조회/결과 조회 API
- [ ] 비동기 처리 설계
  - [ ] 작업 큐 모델
  - [ ] 워커 실행/재시도/백오프
  - [ ] 작업 상태(`queued`, `running`, `success`, `failed`) 관리
- [ ] DB 스키마 설계
  - [ ] 크롤링 결과 캐시 테이블
  - [ ] 마지막 성공시각/만료시각(`expires_at`) 컬럼
  - [ ] 인덱스(URL 해시, 만료시각, 상태)
- [ ] 보안 설계
  - [ ] SSRF 방어
  - [ ] 허용/차단 도메인 정책
  - [ ] 응답 크기 제한

## 구현 TODO
- [ ] 내부 crawler API 엔드포인트 구현.
- [ ] 비동기 작업 큐/워커 구현.
- [ ] 크롤링 결과 캐시 테이블 생성(evolution 포함).
- [ ] 캐시 만료 6개월 정책 적용.
- [ ] 캐시 히트 시 즉시 응답, 만료/미존재 시 비동기 재수집.
- [ ] 기존 외부 API 호출부를 내부 API 호출로 교체.
- [ ] 관리자 캐시 관리 기능 구현
  - [ ] URL별 캐시 조회
  - [ ] 캐시 삭제
  - [ ] 강제 재크롤링 트리거
- [ ] 실패/예외 시 에러 메시지 및 재처리 규칙 정리.

## 테스트 TODO
- [ ] 단위 테스트: URL 파싱, 에러 매핑, 만료 계산(6개월).
- [ ] 통합 테스트: 대표 외부 링크 케이스 회귀 테스트.
- [ ] 비동기 테스트: 큐 적재/워커 처리/상태 전이 검증.
- [ ] 캐시 테스트: 히트/미스/만료/강제갱신 시나리오 검증.
- [ ] 회귀 테스트: 기존 외부 서비스 결과와 비교 검증.

## 마이그레이션/릴리즈 TODO
- [ ] 기능 플래그로 점진 전환 가능하게 구성.
- [ ] 스테이징에서 외부/내부 결과 비교 기간 운영.
- [ ] 프로덕션 카나리 배포 후 전면 전환.
- [ ] 외부 crawler 의존 설정/인프라 정리.

## 오픈 이슈
- [ ] crawler 원본 저장소와의 동기화 전략은 무엇이 적절한가?
- [ ] 비동기 큐 장애 시 복구/재처리 기준은 어떻게 둘 것인가?
- [ ] 캐시 관리 기능의 권한 범위를 관리자 전용으로 고정할 것인가?

## 완료 기준 (Definition of Done)
- [ ] AhaWiki에서 외부 crawler 서비스 호출이 제거됨.
- [ ] 내부 API + 비동기 처리로 크롤링이 안정적으로 동작함.
- [ ] 크롤링 결과 캐시 테이블 및 6개월 만료 정책이 적용됨.
- [ ] 관리자 캐시 관리 기능(조회/삭제/강제갱신)이 동작함.
- [ ] 테스트(단위/통합/비동기/캐시/회귀) 통과.
