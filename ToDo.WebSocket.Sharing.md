# ToDo: 이중화 환경에서 WebSocket 커서 공유

## 1) 목표
- [ ] 서로 다른 인스턴스에 붙어 있는 사용자라도, 같은 위키 페이지를 보고 있으면 서로의 커서를 실시간에 가깝게 볼 수 있게 한다.
- [ ] 기능을 한 번에 크게 바꾸지 않고, 장애 위험을 낮춘 점진적 방식으로 배포한다.
- [ ] 본 범위는 "커서 위치 공유"로 한정하며, 문서 본문의 실시간 동시편집(OT/CRDT)은 별도 과제로 분리한다.

## 2) 현재 문제 정리
- [ ] 현재 WebSocket 세션/브로드캐스트가 인스턴스 로컬 메모리에만 존재한다.
- [ ] 인스턴스 A/B 이중화 환경에서 A에 붙은 사용자의 커서 이벤트가 B 사용자에게 전달되지 않는다.
- [ ] 결과적으로 같은 페이지 참여자 간 커서 동기화가 인스턴스 경계를 넘지 못한다.

## 3) 대상 아키텍처(점진 도입)
- [ ] 소켓 연결 관리는 각 인스턴스 로컬에 유지한다.
- [x] 인스턴스 간 이벤트 중계만 Redis Pub/Sub로 처리한다.
- [x] 페이지 단위 채널로 fan-out 범위를 제한한다. (예: `ws:wiki:{pageId}:cursor`)
- [ ] 커서 이벤트는 일시적 데이터로 취급하고(Pub/Sub), 내구성 저장은 요구하지 않는다.

## 4) 이벤트 계약(커서)
- [x] 공통 JSON Envelope를 정의하고 서버/클라이언트가 동일 스키마를 사용한다.
- [ ] 필수 필드
  - [x] `eventId`: 중복 수신 방지(디듀프)용 고유 ID
  - [x] `type`: 이벤트 타입 (`cursor.move`)
  - [x] `pageId`: 페이지 식별자
  - [x] `userId`: 사용자 식별자 (항상 포함, 포인터 식별은 `senderId` 기준)
  - [x] `x`, `y`: 정규화 좌표(0..1)
  - [x] `ts`: 이벤트 발생 시각(Unix ms)
  - [x] `originInstanceId`: 이벤트 발행 인스턴스 ID
- [ ] 예시 페이로드

```json
{
  "eventId": "6f4f9ec6-1e41-4a88-bec9-987dd8f9c1d1",
  "type": "cursor.move",
  "pageId": "wiki-123",
  "userId": "u-45",
  "x": 0.42,
  "y": 0.18,
  "ts": 1770000000000,
  "originInstanceId": "app-1"
}
```

## 5) 점진 작업 계획 (체크리스트)

### Phase 0 — 사전 정렬(요구사항/안전성)
- [ ] 범위를 커서 공유로 고정하고 비범위(실시간 동시편집)를 문서화한다.
- [ ] 비기능 요구사항 합의: 지연 목표, 허용 가능한 일시 유실, 페이지당 동시 접속자 상한.
- [ ] 개인정보/프라이버시 정책 확인: "커서 공유 끄기" 옵션 필요 여부 확정.

### Phase 1 — 백엔드 Pub/Sub 기반 추가
- [x] WebSocket 수신 이벤트를 Redis에 publish하는 경로를 추가한다.
- [x] 각 인스턴스에서 Redis subscribe 워커를 기동한다.
- [x] subscribe 수신 이벤트를 로컬 페이지 룸 브로드캐스트로 전달한다.
- [x] 루프 방지: subscribe로 받은 이벤트는 재-publish하지 않는다.
- [x] `originInstanceId`를 로그/디버깅에 포함한다.

### Phase 2 — 페이지 룸 라우팅 정비
- [ ] 페이지 단위 룸 키 규칙을 표준화한다.
- [ ] 룸 join 시 페이지 읽기 권한을 서버에서 검증한다.
- [ ] 연결 종료 시 leave/정리 로직을 보장한다.
- [ ] 재연결 시 기존 페이지 룸 자동 재참여를 보장한다.

### Phase 3 — 클라이언트 전송 품질
- [x] 커서 move 이벤트 전송 주기를 30~60ms로 throttle한다.
- [x] 수신 렌더링에 보간(interpolation)/스무딩을 적용한다.
- [x] 자기 자신의 이벤트는 렌더링에서 제외한다.
- [x] heartbeat 누락 시 stale 커서를 자동 제거한다.

### Phase 4 — Presence/안정성 강화
- [ ] Redis TTL 기반 presence heartbeat를 추가한다. (예: TTL 10~15초)
- [ ] 만료 사용자/고아 커서를 정리한다.
- [ ] subscriber 재연결(backoff) 로직을 추가한다.
- [ ] 캐시용 Redis 연결과 Pub/Sub 연결 전략을 분리한다.

### Phase 5 — 관측성(Observability)
- [ ] 메트릭 수집
  - [ ] publish TPS
  - [ ] subscribe 수신 TPS
  - [ ] 페이지별 fan-out 수
  - [ ] subscriber 재연결 횟수/실패 수
- [ ] 구조화 로그 필드 통일 (`eventId`, `pageId`, `instanceId`, `userId`)
- [ ] 이상 징후 알람 기준 정의(구독 끊김, 수신 급감, 지연 급증)

### Phase 6 — 검증(테스트 시나리오)
- [ ] 로컬에서 앱 2인스턴스 + Redis 1개 구성 검증.
- [ ] 교차 인스턴스 시나리오 검증 (사용자 A=instance-1, 사용자 B=instance-2).
- [ ] 버스트 트래픽(마우스 급이동) 부하 sanity 테스트.
- [ ] 장애 훈련: 인스턴스 재시작 후 자동 복구 확인.

### Phase 7 — 배포/롤백
- [x] 기능 플래그 `cursor-sharing-via-redis` 추가.
- [ ] 스테이징에서 선적용 후 메트릭/로그 기준 충족 확인.
- [ ] 프로덕션 점진 오픈(소수 트래픽 → 전체).
- [ ] 롤백 절차 문서화(플래그 OFF 시 동작 포함).

## 6) 보안/권한 체크
- [ ] 페이지 구독 전 서버 권한 검증을 강제한다.
- [ ] 사용자 입력(`pageId`, 좌표값)에 대한 유효성 검사를 수행한다.
- [ ] 채널 네이밍 규칙 외 임의 채널 구독/발행을 차단한다.

## 7) 리스크 및 대응
- [ ] 리스크: 마우스 이벤트 과다로 인한 트래픽 폭증
  - [ ] 대응: throttle, 이벤트 coalescing, 페이지당 상한
- [ ] 리스크: subscriber 연결 끊김으로 인한 일시 동기화 공백
  - [ ] 대응: 재연결 backoff + 헬스체크 + 알람
- [ ] 리스크: 권한 없는 페이지 구독
  - [ ] 대응: join 시 서버 권한검사 + 감사 로그

## 8) 완료 기준(Definition of Done)
- [ ] 서로 다른 인스턴스 사용자 간 동일 페이지 커서가 실시간에 가깝게 보인다.
- [ ] publish/subscribe 경로에 무한 루프가 없다.
- [ ] 기대 트래픽에서 서비스 안정성이 유지된다.
- [ ] 운영자가 원인 분석 가능한 수준의 메트릭/로그가 확보된다.
- [ ] 기능 플래그로 안전하게 ON/OFF 가능하다.

## 9) 비고
- [ ] 본 문서는 구현 체크리스트이며, 상세 코드 설계(클래스/인터페이스/예외 처리)는 별도 설계 문서로 분리한다.
