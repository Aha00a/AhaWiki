= [Interpreter]Math
[AhaWiki] uses [https://www.mathjax.org/ mathjax].
MathJax 수식을 따로 한 줄에 표시합니다. 인라인 수식으로 조판되므로, 분수·합 기호를 크게 쓰려면 앞에 `\displaystyle` 을 붙입니다.
여러 줄 수식을 그대로 적을 수 있습니다.

[[[#!WikiSyntaxPreview Math
\text{cosine similarity}
= \cos(\theta)
= {A \cdot B \over \|A\| \|B\|}
= \frac{ \sum\limits_{i=1}^{n}{A_i\times B_i} }{ \sqrt{\sum\limits_{i=1}^{n}{(A_i)^2}} \times \sqrt{\sum\limits_{i=1}^{n}{(B_i)^2}} }
]]]
