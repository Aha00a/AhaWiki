= [Macro]MonthName
[Macro]

월(`--MM` 형식)에 해당하는 월 이름을 현재 로케일로 출력합니다.

== 사용법
`[[MonthName]]` - 현재 페이지 이름에서 월을 파싱
`[[MonthName(--01)]]` - 1월

인자는 `--MM` 형식(앞에 `--` 접두사)으로 전달합니다.

== 관련
 * [MacroWeekdayName]
