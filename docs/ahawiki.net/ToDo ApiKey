= [ToDo] ApiKey

API Key 기반 Bot API 구현은 완료했다.
상세 설계와 구현 결과는 [Dev ApiKey], 호출 명세는 [BotApi]를 기준 문서로 둔다.

== 남은 작업

 * [[CB]] 1. Account Settings에서 API Key 발급 flow를 staging에서 확인한다.
  * API Key 생성
  * 생성 직후 plain text key 1회 표시와 복사
  * 목록에서 `keyPrefix`, label, 사용/폐기 상태 표시
  * 폐기 후 같은 key로 Bot API 호출 시 `401 Unauthorized`
 * [[CB]] 2. 자동화 도구에서 실제 API 호출 테스트를 수행한다.
  * `GET /api/bot/page/*name`으로 페이지 읽기
  * `POST /api/bot/page/*name`으로 페이지 저장
  * revision 충돌 시 `409 Conflict`
  * 권한 없는 페이지에서 `403 Forbidden`
 * [[CB]] 3. AhaWikiDoc sync에 필요한 Bot API를 보강한다.
  * 페이지 목록/메타 API: `name`, `revision`, `dateTime`, `isMinorEdit`, `viaApi`, content hash를 반환
  * 최근 변경 API: Bot API 인증으로 page prefix, timestamp, revision 기준 필터 지원
  * batch metadata API: 여러 page name의 `revision`, `dateTime`, hash를 한 번에 조회
  * 이름변경 API: local file rename을 원격 page rename으로 반영
  * 삭제 API: local file delete를 원격 page delete로 반영하되 revision 확인과 명시적 확인 절차를 둠
  * sync manifest 전략: `lastSyncedRevision`/`lastSyncedAt` 또는 local manifest 갱신 방식 결정
  * 구현 위치: `conf/routes`, `app/controllers/BotApi.scala`, `test/com/aha00a/controllers/BotApiSpec.scala`, `BotApi`
== 완료/기준 문서

 * [[CB(x)]] 구현 설계와 완료 내역: [Dev ApiKey]
 * [[CB(x)]] Bot API 호출 명세: [BotApi]
 * [[CB(x)]] Account Settings API Key UI의 light/dark theme 적용과 자동화 도구 placeholder 정리
 * [[CB(x)]] Account Settings를 위키 문서형 레이아웃과 `wikiTableSimple` 테이블 스타일로 정리
 * [[CB(x)]] Account Settings와 권한 없음 Login 버튼의 색상/글꼴 크기를 사이트 기본 톤에 맞춤
 * [[CB(x)]] Bot 저장 시 websocket broadcast와 Telegram 알림은 보내지 않는다.
  * Bot 저장 API는 page 저장, cache invalidate, page calculation enqueue까지만 수행한다.
  * Telegram 알림은 `minorEdit`와 `viaApi` 저장을 제외한다.
